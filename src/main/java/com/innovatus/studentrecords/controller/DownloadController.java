package com.innovatus.studentrecords.controller; import com.innovatus.studentrecords.model.Student; import com.innovatus.studentrecords.service.*; import java.nio.file.*; import java.util.*; public class DownloadController {public Path exportAndEmail(String email,List<Student>s)throws Exception{if(s.isEmpty())throw new IllegalArgumentException("No matching records.");Path p=new ExcelService().write(s);new EmailService().export(email,p);return p;}}
